#include <iostream>
#include <math.h>
using namespace std;
int main(void)
{
	float x, b, z;
	x = 3.8;
	b = -4.82;
	z = pow(sqrt((x + 1) / (x - 1)), 3) + 2 * b * log(cos(pow(4 - x, 1.0 / 4.0)) + ctg(b) - (log10(pow(x, 2) / (2 * 3 * 4 * 5));
	cout << "Z = " << z;
}